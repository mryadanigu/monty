#include "monty.h"

/**
 * nop - does nothing
 * @stack: pointer to the head of the stack
 * @line_number: line number in the file
 *
 * Return: no return
 */
void nop(stack_t **stack, unsigned int line_number)
{
	(void)stack;
	(void)line_number;
	/* Implementation of nop opcode (does nothing) */
}

